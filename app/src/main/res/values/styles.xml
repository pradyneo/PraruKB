<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/candidate_normal</item>
        <item name="colorPrimaryDark">@color/candidate_background</item>
        <item name="colorAccent">@color/candidate_other</item>
    </style>

</resources>
